extends layout

block content
  div(class="details", id="branch-bound-result")
    h1 RESULT
    - var sA = a;
    - var sB = b;
    - var output = out;
    p #[strong a = ] #{sA}
    p #[strong b = ] #{sB}
    p #[strong ANSWER: ]
    pre #{output}
